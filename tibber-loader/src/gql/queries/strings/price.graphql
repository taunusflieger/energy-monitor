query Price($id: ID!) {
    viewer {
        home(id: $id) {
            currentSubscription {
                priceInfo {
                    current {
                        total
                        energy
                        tax
                        startsAt
                        currency
                        level
                    }
                }
            }
        }
    }
}
